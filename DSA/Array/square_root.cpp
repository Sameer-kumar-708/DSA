#include <iostream>
using namespace std;

int square_root(int n)
{

  int start = 1;
  int end = x;

  while (start <= end)
  {

    int mid = start + (end - start) / 2;

    if (mid * mid == x)
    {
      return mid;
    }
    else if (mid * mid < x)
    {
    }
  }
}

int main()
{

  int n = 81;

  int arr[n];

  int ans = square_root(arr[], n);
  cout << and;

  return 0;
}